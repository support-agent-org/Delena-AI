# OpenCode Server URL (required)
# This should point to your running OpenCode server instance
OPENCODE_URL=http://127.0.0.1:4096

# Model Selection (optional)
# If not set, the OpenCode server will use its configured default model
# Format: provider/model (e.g., "google/gemini-2.5-flash", "firmware/gpt-5-nano")
# Leave empty or comment out to use server default
# MODEL=
# To use Google Gemini models, add your API key to the .env file like:
#   GOOGLE_GENERATIVE_AI_API_KEY=your_key 
#
# Or set it in the OpenCode server's environment before starting
